First run the command to install dependencies
npm install

start the server as:
node replica.js directory_to_serve port

localhost:port/fetchResources --> gives the list of files in the "directory_to_serve"